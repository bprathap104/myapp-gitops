global:
  environment: dev

deployment:
  replicaCount: 1

image:
  tag: "latest"

service:
  type: ClusterIP

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

externalSecrets:
  database:
    remoteKey: myapp/database

env:
  static:
    NODE_ENV: development
    LOG_LEVEL: debug
